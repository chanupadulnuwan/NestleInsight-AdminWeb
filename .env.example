# Leave this blank when the web app and API are served from the same origin.
# Do not set this to an http:// URL for an https:// site, or the browser will
# block the requests as mixed content.
VITE_API_BASE_URL=

# Local Vite dev proxy target.
VITE_API_PROXY_TARGET=http://localhost:3000
